# OpenRouter API Key (required)
OPENROUTER_API_KEY={get from https://openrouter.ai/settings/keys}

# Optional settings
# NOMOUNT=1         # Set to 1 to not mount current directory to /app in container
# FORCE_YES=1       # Set to 1 to add --yes-always flag to aider in container
